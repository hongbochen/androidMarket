<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- android:shape="" 表示是圆角矩形还是椭圆等等 -->
    <solid android:color="#ffffff" />
    <!-- 背景颜色 -->


    <!-- padding 表示内部空间距离背景图片内部边距 的距离 -->
    <padding
        android:bottom="5dp"
        android:left="10dp"
        android:right="10dp"
        android:top="5dp" />

    <stroke
        android:width="1.5dp"
        android:color="#fb9849" />
    <!-- 图片边框属性 -->
    <corners android:radius="10dp" />
    <!-- 圆角的程度 -->
    <gradient
        android:angle="90"
        android:centerColor="@color/mgrey"
        android:centerX="0.5"
        android:centerY="0.5"
        android:endColor="@color/mgrey"
        android:startColor="@color/mgrey"
        android:type="linear" />

</shape>